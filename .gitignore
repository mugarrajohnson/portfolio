# Dependencies. Note assets/styles.css IS committed: the site deploys
# as plain static files and must work without a build step on the host.
node_modules/

# Private source material — never commit or deploy
New Folder/
*.rar
*.zip

# Working/draft files
PROJECTS_DRAFT.md
build_cv.js

# Old / superseded CVs — only the standard CV is public
CV_Johnson_Mugarra.pdf
Johnson_Mugarra.pdf

# Large original hero (optimized versions are committed)
img/Vanilla Workshop.JPG

# OS / editor cruft
.DS_Store
Thumbs.db

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
